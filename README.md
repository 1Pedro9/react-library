# react-library
A library of components that I can use in future react projects


## Process of installment
1. `npm init -y`
2. Install the dependencies
```bash
npm install react react-dom
npm install -D tsup
```